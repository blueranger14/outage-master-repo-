name: Process Zip Outage Source

# Trigger bila fail .zip baru di-push ke inbox/zip_history/
on:
  push:
    paths:
      - "inbox/zip_history/*.zip"
  # Boleh trigger manual juga dari tab "Actions" di GitHub (untuk testing/debug)
  workflow_dispatch:

# Perlu "contents: write" supaya workflow boleh commit balik perubahan
# (master_outage.xlsx yang diupdate + fail zip yang dipindah ke processed/)
permissions:
  contents: write

jobs:
  process-zip-source:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run merge_zip_to_master.py
        run: python scripts/merge_zip_to_master.py

      - name: Check for changes
        id: git_check
        run: |
          if [ -n "$(git status --porcelain)" ]; then
            echo "changes=true" >> "$GITHUB_OUTPUT"
          else
            echo "changes=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Commit dan push perubahan
        if: steps.git_check.outputs.changes == 'true'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add master/master_outage.xlsx inbox/zip_history/ processed/zip_history/
          git commit -m "Auto-update master outage (zip source) [skip ci]"
          git push

      - name: Tiada perubahan
        if: steps.git_check.outputs.changes == 'false'
        run: echo "Tiada fail baru diproses, master tak berubah."
